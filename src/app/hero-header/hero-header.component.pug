#header([style.height.px]="getHeaderHeight()")
  #header-background([style.opacity]="getOpacity(600)")
  .ie10-fix
    #header-centered
      h1 {{Name}}
  #header-bottom([style.opacity]="getOpacity(450)")
    h2 {{Tag}}